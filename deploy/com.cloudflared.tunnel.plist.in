<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- cloudflared 用户级 LaunchAgent 模板:@占位符@ 由 `make install-tunnel` 渲染。
     不用 cloudflared 自带的 service install:它生成的 plist 不带 `tunnel run` 子命令,
     对 config.yml 里的命名隧道会直接 exit 1,陷入 launchd 重启循环。 -->
<plist version="1.0">
<dict>
    <!-- Label 不用 com.neve.*:隧道是机器级设施(可服务多站点),且避免与
         com.neve.server 在 svc 等按第二段取短名的工具里撞名 -->
    <key>Label</key>
    <string>com.cloudflared.tunnel</string>

    <key>ProgramArguments</key>
    <array>
        <string>@CLOUDFLARED_BIN@</string>
        <string>tunnel</string>
        <string>run</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <!-- 崩溃后自动重启,正常退出不拉起(与 com.neve.server 约定一致) -->
    <key>KeepAlive</key>
    <dict>
        <key>SuccessfulExit</key>
        <false/>
    </dict>

    <key>ThrottleInterval</key>
    <integer>10</integer>

    <key>ProcessType</key>
    <string>Background</string>

    <key>StandardOutPath</key>
    <string>@HOME@/Library/Logs/cloudflared.log</string>

    <key>StandardErrorPath</key>
    <string>@HOME@/Library/Logs/cloudflared.error.log</string>
</dict>
</plist>
